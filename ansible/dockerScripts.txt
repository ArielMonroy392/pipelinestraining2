docker network create ansible

docker run -d --name administrated --network ansible rastasheep/ubuntu-sshd

docker run -it --name admin --network ansible ubuntu bash

docker start -ai admin

inside admin container

apt update && apt install -y openssh-client

apt install ansible

ansible-playbook -i hosts.ini setup_apache.yml